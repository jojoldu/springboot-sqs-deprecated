spring:
  profiles:
    active: local

cloud:
  aws:
    region:
      static: ap-northeast-2

sqs:
  mock:
    enabled: true
    host: localhost
    port: 9000
  queueNames: {
    "basic": "basic",
    "basicDlq": "basic-dlq",
    "point": "point",
    "pointDlq": "point-dlq"
  }
